<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Legacy aliases pointing to DesignSystem for backward compatibility -->
    <SolidColorBrush x:Key="BgBrush" Color="{StaticResource DS_Background}" />
    <SolidColorBrush x:Key="PanelBrush" Color="{StaticResource DS_SecondaryBg}" />
    <SolidColorBrush x:Key="Panel2Brush" Color="{StaticResource DS_PanelSurface}" />
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource DS_Border}" />
    <SolidColorBrush x:Key="TextBrush" Color="{StaticResource DS_PrimaryText}" />
    <SolidColorBrush x:Key="MutedBrush" Color="{StaticResource DS_SecondaryText}" />
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource DS_Accent}" />
    <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource DS_Danger}" />
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource DS_Warning}" />

    <!-- Base Styles -->
    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource DS_BackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource DS_PrimaryTextBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="13" />
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource DS_PrimaryTextBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="13" />
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource DS_PrimaryTextBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
    </Style>

    <Style TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource DS_PrimaryTextBrush}" />
        <Setter Property="Background" Value="{StaticResource DS_PanelSurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource DS_BorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="Margin" Value="4" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FontFamily" Value="Segoe UI" />
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="Foreground" Value="{StaticResource DS_PrimaryTextBrush}" />
        <Setter Property="Background" Value="{StaticResource DS_SecondaryBgBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource DS_BorderBrush}" />
        <Setter Property="Padding" Value="8" />
        <Setter Property="Margin" Value="4" />
        <Setter Property="FontFamily" Value="Segoe UI" />
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Foreground" Value="{StaticResource DS_PrimaryTextBrush}" />
        <Setter Property="Background" Value="{StaticResource DS_PanelSurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource DS_BorderBrush}" />
        <Setter Property="Margin" Value="4" />
        <Setter Property="Padding" Value="10,6" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton"
                                      Focusable="False"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press"
                                      Background="{StaticResource DS_PanelSurfaceBrush}"
                                      BorderBrush="{StaticResource DS_BorderBrush}"
                                      BorderThickness="1">
                            <Grid>
                                <ContentPresenter Margin="10,0,30,0"
                                                  HorizontalAlignment="Left"
                                                  VerticalAlignment="Center"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" />
                                <TextBlock Text="v"
                                           Foreground="{StaticResource DS_PrimaryTextBrush}"
                                           HorizontalAlignment="Right"
                                           VerticalAlignment="Center"
                                           Margin="0,0,10,0" />
                            </Grid>
                        </ToggleButton>
                        <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
                               Placement="Bottom"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Fade">
                            <Border Background="{StaticResource DS_SecondaryBgBrush}"
                                    BorderBrush="{StaticResource DS_BorderBrush}"
                                    BorderThickness="1"
                                    MinWidth="{TemplateBinding ActualWidth}">
                                <ScrollViewer MaxHeight="220">
                                    <ItemsPresenter />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="{StaticResource DS_PrimaryTextBrush}" />
        <Setter Property="Background" Value="{StaticResource DS_SecondaryBgBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="Padding" Value="10,6" />
    </Style>

    <Style TargetType="TabItem">
        <Setter Property="Foreground" Value="{StaticResource DS_PrimaryTextBrush}" />
        <Setter Property="Background" Value="{StaticResource DS_PanelSurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource DS_BorderBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource DS_PrimaryTextBrush}" />
        <Setter Property="Background" Value="{StaticResource DS_PanelSurfaceBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
    </Style>

    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource DS_SecondaryBgBrush}" />
        <Setter Property="Foreground" Value="{StaticResource DS_PrimaryTextBrush}" />
        <Setter Property="RowBackground" Value="{StaticResource DS_PanelSurfaceBrush}" />
        <Setter Property="AlternatingRowBackground" Value="{StaticResource DS_SecondaryBgBrush}" />
        <Setter Property="GridLinesVisibility" Value="Horizontal" />
        <Setter Property="BorderBrush" Value="{StaticResource DS_BorderBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
    </Style>
</ResourceDictionary>
